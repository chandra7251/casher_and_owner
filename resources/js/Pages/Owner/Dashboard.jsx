import { Head } from '@inertiajs/react';

const money = (value) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(value);

export default function Dashboard({ data }) {
    return <><Head title="Dashboard Owner" /><main className="min-h-screen bg-cream p-5 text-ink sm:p-8"><header className="mb-8"><p className="text-sm font-semibold text-terracotta">Owner</p><h1 className="font-display text-3xl font-bold text-espresso">Ringkasan kedai</h1><p className="mt-2 text-muted">Angka transaksi dan stok dari database.</p></header><section className="grid gap-4 sm:grid-cols-3"><article className="rounded-2xl bg-espresso p-5 text-white"><p className="text-sm text-cream/70">Pendapatan paid</p><strong className="mt-2 block font-display text-2xl">{money(data.paid_revenue)}</strong></article><article className="rounded-2xl border border-line bg-white p-5"><p className="text-sm text-muted">Order paid</p><strong className="mt-2 block font-display text-2xl text-espresso">{data.paid_orders}</strong></article><article className="rounded-2xl border border-line bg-white p-5"><p className="text-sm text-muted">Order expired</p><strong className="mt-2 block font-display text-2xl text-danger">{data.expired_orders}</strong></article></section><section className="mt-6 rounded-2xl border border-line bg-white p-5"><h2 className="font-display text-xl font-bold text-espresso">Stok perlu dicek</h2>{data.low_stock.length === 0 ? <p className="mt-4 text-muted">Semua stok aman.</p> : <div className="mt-4 space-y-3">{data.low_stock.map((row) => <div key={row.id} className="flex items-center justify-between rounded-xl bg-cream p-3"><span><strong>{row.menu_item?.name ?? 'Menu'}</strong> · {row.size}</span><span className="font-bold text-danger">{row.on_hand} tersisa</span></div>)}</div>}</section></main></>;
}
