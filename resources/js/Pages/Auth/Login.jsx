import { Head, useForm } from '@inertiajs/react';

export default function Login() {
    const form = useForm({ email: '', password: '' });
    return <main className="flex min-h-screen items-center justify-center bg-cream p-6"><Head title="Masuk Kasir" /><form onSubmit={(event) => { event.preventDefault(); form.post('/login'); }} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl"><p className="text-sm font-semibold text-terracotta">Kedai Senja</p><h1 className="mt-2 font-display text-3xl font-bold text-espresso">Masuk ke POS</h1><p className="mt-2 text-sm text-muted">Gunakan akun kasir atau owner.</p><label className="mt-6 block text-sm font-semibold">Email<input type="email" value={form.data.email} onChange={(event) => form.setData('email', event.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-line px-4 outline-none focus:border-terracotta" autoComplete="username" /></label><label className="mt-4 block text-sm font-semibold">Password<input type="password" value={form.data.password} onChange={(event) => form.setData('password', event.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-line px-4 outline-none focus:border-terracotta" autoComplete="current-password" /></label>{form.errors.email && <p className="mt-3 text-sm text-danger">{form.errors.email}</p>}<button disabled={form.processing} className="mt-6 w-full rounded-xl bg-espresso px-4 py-3 font-bold text-white disabled:opacity-50">{form.processing ? 'Memeriksa…' : 'Masuk'}</button></form></main>;
}
